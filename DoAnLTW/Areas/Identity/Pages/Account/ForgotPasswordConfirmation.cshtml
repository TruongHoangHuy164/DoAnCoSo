@page
@model ForgotPasswordConfirmationModel
@{
    ViewData["Title"] = "Xác nhận quên mật khẩu";
}

<h2 class="text-center">@ViewData["Title"]</h2>
<div class="container mt-4">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card shadow p-4">
                <div class="alert alert-info">
                    Vui lòng kiểm tra email của bạn để nhận mã OTP và đặt lại mật khẩu.
                </div>
                <a class="btn btn-primary" asp-page="./ResetPassword">Nhập mã OTP</a>
                <a class="btn btn-secondary mt-2" asp-page="./Login">Quay lại đăng nhập</a>
            </div>
        </div>
    </div>
</div>