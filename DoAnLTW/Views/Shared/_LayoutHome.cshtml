@using Microsoft.AspNetCore.Authentication
@using Microsoft.AspNetCore.Authentication.Cookies
@using Microsoft.AspNetCore.Identity
@using DoAnLTW.Models

@inject IHttpContextAccessor HttpContextAccessor
@inject SignInManager<IdentityUser> SignInManager
@inject UserManager<IdentityUser> UserManager
@inject ApplicationDbContext _context
<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>@ViewBag.Title</title>
    <meta charset="utf-8">

    @{
        var isAuthenticated = HttpContextAccessor.HttpContext.User.Identity.IsAuthenticated;
    }
    <meta content="Free HTML Templates" name="keywords">
    <meta content="Free HTML Templates" name="description">

    <!-- Favicon -->
    <link href="img/favicon.ico" rel="icon">

    <!-- Google Web Fonts -->
    <link rel="preconnect" href="https://fonts.gstatic.com">
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;500;700&display=swap" rel="stylesheet">

    <!-- Font Awesome -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.10.0/css/all.min.css" rel="stylesheet">

    <!-- Libraries Stylesheet -->
    <link href="~/lib/animate/animate.min.css" rel="stylesheet">
    <link href="~/lib/owlcarousel/assets/owl.carousel.min.css" rel="stylesheet">

    <!-- Customized Bootstrap Stylesheet -->
    <link href="~/css/style.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">

    <!-- CSS cho nút microphone -->
    <style>
        .search-container {
            display: flex;
            align-items: center;
            gap: 10px;
        }

            .search-container .btn-mic {
                background-color: #000000;
                border: none;
                color: #FFFFFF;
                padding: 8px 12px;
                border-radius: 5px;
                cursor: pointer;
                transition: background-color 0.3s ease;
            }

                .search-container .btn-mic:hover {
                    background-color: #333333;
                }

                .search-container .btn-mic.recording {
                    background-color: #FF0000;
                }

                    .search-container .btn-mic.recording:hover {
                        background-color: #CC0000;
                    }
    </style>
</head>
<body>
    <div>
        <!-- Topbar Start -->
        <div class="container-fluid">
            <div class="row bg-secondary py-1 px-xl-5">
                <div class="col-lg-6 d-none d-lg-block">
                    <div class="d-inline-flex align-items-center h-100">
                        <a class="text-body mr-3" href="">About</a>
                        <a class="text-body mr-3" href="">Contact</a>
                        <a class="text-body mr-3" href="">Help</a>
                        <a class="text-body mr-3" href="">FAQs</a>
                    </div>
                </div>
                <div class="col-lg-6 text-center text-lg-right">
                    <div class="d-inline-flex align-items-center">
                        <div class="btn-group">
                            @if (SignInManager.IsSignedIn(User))
                            {
                                <button type="button" class="btn btn-sm btn-light dropdown-toggle" data-toggle="dropdown">
                                    <i class="fas fa-user"></i> @User.Identity.Name
                                </button>
                            }
                            else
                            {
                                <button type="button" class="btn btn-sm btn-light dropdown-toggle" data-toggle="dropdown">Tài khoản</button>
                            }
                            <div class="dropdown-menu dropdown-menu-right">
                                @if (SignInManager.IsSignedIn(User))
                                {
                                    <h6 class="dropdown-header">Xin chào, @User.Identity.Name!</h6>
                                    <div class="dropdown-divider"></div>
                                    <a class="dropdown-item" href="#">
                                        <i class="fas fa-user-circle mr-2"></i>Thông tin tài khoản
                                    </a>
                                    <a class="dropdown-item" href="#">
                                        <i class="fas fa-shopping-bag mr-2"></i>Đơn hàng của tôi
                                    </a>
                                    <div class="dropdown-divider"></div>
                                    <form asp-controller="Account" asp-action="Logout" method="post">
                                        <button type="submit" class="dropdown-item text-danger">
                                            <i class="fas fa-sign-out-alt mr-2"></i>Đăng xuất
                                        </button>
                                    </form>
                                }
                                else
                                {
                                    <a class="dropdown-item" asp-area="Identity" asp-page="/Account/Login">
                                        <i class="fas fa-sign-in-alt mr-2"></i>Đăng nhập
                                    </a>
                                    <a class="dropdown-item" asp-area="Identity" asp-page="/Account/Register">
                                        <i class="fas fa-user-plus mr-2"></i>Đăng ký
                                    </a>
                                    <div class="dropdown-divider"></div>
                                    <a asp-controller="Account" asp-action="LoginByGoogle" class="dropdown-item">
                                        <i class="fab fa-google mr-2"></i>Đăng nhập bằng Google
                                    </a>
                                }
                            </div>
                        </div>
                        <div class="btn-group mx-2">
                            <button type="button" class="btn btn-sm btn-light dropdown-toggle" data-toggle="dropdown">VNĐ</button>
                            <div class="dropdown-menu dropdown-menu-right">
                                <button class="dropdown-item" type="button">USA</button>
                            </div>
                        </div>
                        <div class="btn-group">
                            <button type="button" class="btn btn-sm btn-light dropdown-toggle" data-toggle="dropdown">VN</button>
                            <div class="dropdown-menu dropdown-menu-right">
                                <button class="dropdown-item" type="button">ENG</button>
                            </div>
                        </div>
                    </div>
                    <div class="d-inline-flex align-items-center d-block d-lg-none">
                        <a href="" class="btn px-0 ml-2">
                            <i class="fas fa-heart text-dark"></i>
                            <span class="badge text-dark border border-dark rounded-circle" style="padding-bottom: 2px;">0</span>
                        </a>
                        <a href="" class="btn px-0 ml-2">
                            <i class="fas fa-shopping-cart text-dark"></i>
                            <span class="badge text-dark border border-dark rounded-circle" style="padding-bottom: 2px;">0</span>
                        </a>
                    </div>
                </div>
            </div>
            <div class="row align-items-center bg-light py-3 px-xl-5 d-none d-lg-flex">
                <div class="col-lg-4">
                    <a href="" class="text-decoration-none">
                        <span class="h1 text-uppercase text-primary bg-dark px-2">PET</span>
                        <span class="h1 text-uppercase text-dark bg-primary px-2 ml-n1">LOVER</span>
                    </a>
                </div>
                <div class="col-lg-4 col-6 text-left">
                    <form asp-controller="Shop" asp-action="Index" method="get" class="search-container">
                        <div class="input-group">
                            <input type="text" class="form-control" name="searchTerm" id="searchInput" placeholder="Tìm kiếm sản phẩm..." value="@Context.Request.Query["searchTerm"]">
                            <div class="input-group-append">
                                <button class="btn btn-primary" type="submit">
                                    <i class="fa fa-search"></i>
                                </button>
                                <button type="button" class="btn btn-mic" id="micButton" title="Tìm kiếm bằng giọng nói">
                                    <i class="fas fa-microphone"></i>
                                </button>
                            </div>
                        </div>
                    </form>
                </div>
                <div class="col-lg-4 col-6 text-right">
                    <p class="m-0">Chăm sóc khách hàng</p>
                    <h5 class="m-0">(+84)339542115</h5>
                </div>
            </div>
        </div>
        <!-- Topbar End -->
        <!-- Navbar Start -->
        <div class="container-fluid bg-dark mb-30">
            <div class="row px-xl-5">
                <div class="col-lg-3 d-none d-lg-block">
                    <a class="btn d-flex align-items-center justify-content-between bg-primary w-100" data-toggle="collapse" href="#navbar-vertical" style="height: 65px; padding: 0 30px;">
                        <h6 class="text-dark m-0"><i class="fa fa-bars mr-2"></i>Phân Loại</h6>
                        <i class="fa fa-angle-down text-dark"></i>
                    </a>
                    <nav class="collapse position-absolute navbar navbar-vertical navbar-light align-items-start p-0 bg-light" id="navbar-vertical" style="width: calc(100% - 30px); z-index: 999;">
                        <div class="navbar-nav w-100">
                            @{
                                var brands = _context.Brands.ToList();
                                var categories = _context.Categories.ToList();
                            }
                            <a asp-controller="Shop" asp-action="Index" class="nav-item nav-link">Tất cả sản phẩm</a>
                            <div class="nav-item dropdown dropright">
                                <a href="#" class="nav-link dropdown-toggle" data-toggle="dropdown">Danh Mục <i class="fa fa-angle-right float-right mt-1"></i></a>
                                <div class="dropdown-menu position-absolute rounded-0 border-0 m-0">
                                    @foreach (var category in categories)
                                    {
                                        <a asp-controller="Shop" asp-action="Index" asp-route-categoryId="@category.CategoryId" class="dropdown-item">@category.Name</a>
                                    }
                                </div>
                            </div>
                            <div class="nav-item dropdown dropright">
                                <a href="#" class="nav-link dropdown-toggle" data-toggle="dropdown">Thương Hiệu <i class="fa fa-angle-right float-right mt-1"></i></a>
                                <div class="dropdown-menu position-absolute rounded-0 border-0 m-0">
                                    @foreach (var brand in brands)
                                    {
                                        <a asp-controller="Shop" asp-action="Index" asp-route-brandId="@brand.BrandId" class="dropdown-item">@brand.Name</a>
                                    }
                                </div>
                            </div>
                        </div>
                    </nav>
                </div>
                <div class="col-lg-9">
                    <nav class="navbar navbar-expand-lg bg-dark navbar-dark py-3 py-lg-0 px-0">
                        <a href="" class="text-decoration-none d-block d-lg-none">
                            <span class="h1 text-uppercase text-dark bg-light px-2">Multi</span>
                            <span class="h1 text-uppercase text-light bg-primary px-2 ml-n1">Shop</span>
                        </a>
                        <button type="button" class="navbar-toggler" data-toggle="collapse" data-target="#navbarCollapse">
                            <span class="navbar-toggler-icon"></span>
                        </button>
                        <div class="collapse navbar-collapse justify-content-between" id="navbarCollapse">
                            <div class="navbar-nav mr-auto py-0">
                                <a asp-controller="Home" asp-action="Index" class="nav-item nav-link">Trang chủ</a>
                                <a asp-controller="Shop" asp-action="Index" class="nav-item nav-link">Sản phẩm</a>
                                <a asp-controller="Contact" asp-action="Index" class="nav-item nav-link">Liên hệ</a>
                                <a asp-controller="CustomerChat" asp-action="Index" class="nav-item nav-link">Hỗ trợ Khách Hàng</a>
                                <li class="nav-item dropdown">
                                    <a class="nav-link dropdown-toggle" href="#" id="petServicesDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                                        <i class="fas fa-paw"></i> Dịch vụ thú cưng
                                    </a>
                                    <div class="dropdown-menu" aria-labelledby="petServicesDropdown">
                                        <a class="dropdown-item" asp-controller="UserPets" asp-action="Index">
                                            <i class="fas fa-dog"></i> Thú cưng của tôi
                                        </a>
                                        <a class="dropdown-item" asp-controller="PetServices" asp-action="Index">
                                            <i class="fas fa-concierge-bell"></i> Đặt dịch vụ
                                        </a>
                                        <a class="dropdown-item" asp-controller="PetServices" asp-action="MyBookings">
                                            <i class="fas fa-calendar-check"></i> Lịch đặt của tôi
                                        </a>
                                       
                                    </div>
                                </li>
                            </div>
                            <div class="navbar-nav ml-auto py-0 d-none d-lg-block">
                                <a asp-controller="Shop" asp-action="FavouriteProducts" class="btn px-0">
                                    <i class="fas fa-heart text-primary"></i>
                                    <span class="badge text-secondary border border-secondary rounded-circle" style="padding-bottom: 2px;">@ViewBag.FavouriteCount</span>
                                </a>
                                <a asp-controller="Cart" asp-action="Index" class="btn px-0 ml-3">
                                    <i class="fas fa-shopping-cart text-primary"></i>
                                    <span class="badge text-secondary border border-secondary rounded-circle" style="padding-bottom: 2px;">@ViewBag.CartCount</span>
                                </a>
                            </div>
                        </div>
                    </nav>
                </div>
            </div>
        </div>
        <!-- Navbar End -->
        @RenderBody()

        @await Html.PartialAsync("_Footer")
    </div>
    <!-- Back to Top -->
    <a href="#" class="btn btn-primary back-to-top"><i class="fa fa-angle-double-up"></i></a>

    <!-- Zalo Button -->
    <a href="https://zalo.me/0932961658" class="btn btn-primary contact-zalo" target="_blank">
        <img src="/img/zalo_icon.png" alt="Zalo" style="width: 20px;">
    </a>

    <!-- JavaScript Libraries -->
    <script src="https://code.jquery.com/jquery-3.4.1.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/js/bootstrap.bundle.min.js"></script>
    <script src="~/lib/easing/easing.min.js"></script>
    <script src="~/lib/owlcarousel/owl.carousel.min.js"></script>

    <!-- Contact Javascript File -->
    <script src="mail/jqBootstrapValidation.min.js"></script>
    <script src="mail/contact.js"></script>
    <script src="https://app.preny.ai/embed-global.js"
            data-button-style="width:200px;height:200px"
            async
            defer
            data-preny-bot-id="6812515e3d3e225ee029d289" />
    <!-- Template Javascript -->
    <script src="~/js/main.js"></script>

    <!-- Voice Search Script -->
    <script>
        $(document).ready(function () {
            function initializeSpeechRecognition() {
                const micButton = document.getElementById('micButton');
                const searchInput = document.getElementById('searchInput');
                let recognition = null;

                if ('SpeechRecognition' in window || 'webkitSpeechRecognition' in window) {
                    recognition = new (window.SpeechRecognition || window.webkitSpeechRecognition)();
                    recognition.lang = 'vi-VN';
                    recognition.interimResults = false;
                    recognition.maxAlternatives = 1;

                    micButton.addEventListener('click', () => {
                        if (micButton.classList.contains('recording')) {
                            recognition.stop();
                        } else {
                            recognition.start();
                            micButton.classList.add('recording');
                            micButton.querySelector('i').classList.remove('fa-microphone');
                            micButton.querySelector('i').classList.add('fa-stop');
                        }
                    });

                    recognition.onresult = (event) => {
                        const transcript = event.results[0][0].transcript;
                        searchInput.value = transcript;
                        // Tự động submit form
                        searchInput.closest('form').submit();
                    };

                    recognition.onend = () => {
                        micButton.classList.remove('recording');
                        micButton.querySelector('i').classList.remove('fa-stop');
                        micButton.querySelector('i').classList.add('fa-microphone');
                    };

                    recognition.onerror = (event) => {
                        console.error('Speech recognition error:', event.error);
                        alert('Đã xảy ra lỗi khi nhận diện giọng nói: ' + event.error);
                        micButton.classList.remove('recording');
                        micButton.querySelector('i').classList.remove('fa-stop');
                        micButton.querySelector('i').classList.add('fa-microphone');
                    };
                } else {
                    micButton.disabled = true;
                    micButton.title = 'Trình duyệt không hỗ trợ tìm kiếm bằng giọng nói';
                    micButton.style.opacity = '0.5';
                    micButton.style.cursor = 'not-allowed';
                }
            }

            initializeSpeechRecognition();
        });
    </script>

    @RenderSection("Scripts", required: false)
</body>
</html>